import type { GeneratorConfigWithDefaultValues } from '../../../config/src/types';
import { BaseTranslation, Locale } from '../../../runtime/src/core';
import { Logger } from '../generator-util';
export declare type GenerateTypesType = GeneratorConfigWithDefaultValues & {
    translations: BaseTranslation | BaseTranslation[];
    locales: Locale[];
};
export declare const generateTypes: (config: GenerateTypesType, logger: Logger) => Promise<boolean>;
